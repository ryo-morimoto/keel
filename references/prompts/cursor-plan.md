Analyze the following task and produce an implementation plan. Do NOT implement anything.

Output ONLY a JSON object:
{"steps":["step1","step2"],"verification_criteria":["WHEN x THEN y"],"files_to_modify":["path/to/file"]}

## Task
{{user_task}}

## Investigation Results
{{investigation}}
